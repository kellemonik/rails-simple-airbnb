<h1> Welcome to Airbnb</h1>

<ul>
  <% @flats.each do |flat| %>
   <li> <%= flat.name %>
   <%= link_to "See details", flat %></li>
  <% end %>
</ul>

<%= link_to "Add a new flat",
  new_flat_path(@flat)%>
